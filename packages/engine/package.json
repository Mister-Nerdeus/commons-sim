{
  "name": "@commons-sim/engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test dist/test.js dist/submodels.test.js dist/semantic.test.js",
    "lint": "echo \"(add eslint later)\"",
    "format": "prettier -w .",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@commons-sim/shared": "workspace:*"
  }
}
