{
  "name": "@commons-sim/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "c8 --reporter=text-summary --check-coverage --lines 60 --functions 60 --branches 50 --statements 60 node --test dist/test.js dist/contracts.test.js",
    "lint": "echo \"(add eslint later)\"",
    "format": "prettier -w .",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.8",
    "zod-to-json-schema": "^3.23.2"
  }
}
