# batch-04 route list

Primary route surface:
- `/` -> comparison workspace (`apps/web/src/ui/App.tsx`)

Primary interaction flows:
- baseline selector
- candidate selector
- metric delta cards
- assumption warnings + registry
- export report action
