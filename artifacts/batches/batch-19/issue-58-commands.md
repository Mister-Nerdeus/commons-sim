# Issue 58 Commands

```text
pnpm --filter @commons-sim/web build
```

Observed: web production build passed after the builder shell was added.

Non-claims: not full editor, no server persistence, no real AI, no professional export.
