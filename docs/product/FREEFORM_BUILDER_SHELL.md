# Freeform Builder Shell

The web shell exposes a first modalized builder surface:

- mode selector
- generated polygon project preview
- validation panel grouped by severity
- module palette placeholders
- ProjectManifestV2 JSON export
- navigation back to the existing scenario/challenge workspace

This is a shell only. It does not implement production drag/drop editing, server persistence, external AI, or professional export.
